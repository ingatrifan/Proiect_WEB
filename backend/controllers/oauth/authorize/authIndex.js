const dropboxAuth =require('./dropbox/dropboxAuth');
const googleAuth = require('./google/googleAuth');
const onedriveAuth =require('./onedrive/onedriveAuth');

module.exports={
    dropboxAuth,googleAuth,onedriveAuth
}